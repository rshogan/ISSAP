# Readme 
This is a 100% vibe-coded, gamified practice test for the ISSAP exam 
The creativity is mine, the architecture and instructions are mine... the writing and artwork are Claude's (art is not sourced from any other material)  
Source exam materials taken from the Google Drive linked on this reddit post:  https://www.reddit.com/r/isc2/comments/1tqlo65/issap_study_materials/  
Please note the source exam / questions themselves were created by Claude according to the author and this is all unlicensed information 
Therefore, there is nothing to attribute to the author - this is all 100% LLM generated  
All graphics were generated by Claude based on my instructions.  They are simple .SVG graphics - not pirated and not generated based on any input resources, files or URLs  

# Usage
Download the project.  
Put it in a folder and run "run.bat" (Windows) or "./run.sh" (macOS / Linux)  
The app should open in your default web browser.  Tested in Google Chrome only.  
Both scripts just start a local Python 3 web server. Nothing is installed.  I haven't tested on mac/linux - and probably won't - but you should be able to get the python server to run even if run.sh is faulty.

## Desktop app (optional)
There is also an Electron wrapper, so it can run in its own window instead of a browser tab.  

To build a Windows installer, Node.js is required:  (can't upload an .exe installer to github, it exceeds a single file size limit)
1. `winget install OpenJS.NodeJS.LTS`  
2. run "build-installer.bat" - it npm installs and writes `dist\ISSAP-Cyber-Redemption-Setup-<version>.exe`  

The installer is per-user, so it does not need admin rights.  It is unsigned, so SmartScreen will warn on first run.  